import {
  app,
  components,
  css,
  modules,
  nitro,
  plugins,
  runtimeConfig,
  ssr,
  typescript,
} from './config';

export default defineNuxtConfig({
  app,
  components,
  css,
  modules,
  nitro,
  plugins,
  runtimeConfig,
  ssr,
  typescript,
});
